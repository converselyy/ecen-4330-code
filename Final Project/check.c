void check() {
	// print initial prompt for value to check with

	int value;
	value = getInput();
	// need two because this is only one nibble
	// OR the two nibbles together to form a byte to write to each RAM location

	// print byte on LCD

	// loop going through each RAM location and writing the input byte
	
	// check the RAM and make sure each value at each location matches the input byte
	// if there's an error, print error message out

	// loop going through each RAM location and writing the complemented input byte

	// check the RAM and make sure each value at each location matches the complemented input byte
	// if there's an error, print error message out
	
	// if it completes with no error, then print out a success message
}